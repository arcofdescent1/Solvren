/**
 * Phase 9 — Execution modes (§6).
 */
export enum ExecutionMode {
  DRY_RUN = "DRY_RUN",
  SUGGEST_ONLY = "SUGGEST_ONLY",
  APPROVAL_REQUIRED = "APPROVAL_REQUIRED",
  BOUNDED_AUTO = "BOUNDED_AUTO",
  FULL_AUTO = "FULL_AUTO",
}
