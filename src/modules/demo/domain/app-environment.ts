/**
 * Phase 8 — Demo System.
 * Environment enum used everywhere for demo segregation.
 */
export enum AppEnvironment {
  PRODUCTION = "production",
  STAGING = "staging",
  SANDBOX = "sandbox",
  DEMO = "demo",
  INTERNAL = "internal",
}
