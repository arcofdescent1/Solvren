/**
 * Phase 8 — Demo System.
 * Integration mode for demo orgs — no live writes.
 */
export enum DemoIntegrationMode {
  SEEDED = "SEEDED",
  STUBBED = "STUBBED",
  MIRRORED_READONLY = "MIRRORED_READONLY",
}
